import numpy as np

class Function(object):

    #returns value of function
    def eval(self, x):
        #y = f(x)
        return y

    #returns value of derivative of f(x)
    #this is not numerically found
    #only if know beforehand
    def deriv(self, x):
        #yPrime = f'(x)
        return yPrime
